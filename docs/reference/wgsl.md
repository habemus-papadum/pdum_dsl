# API Reference — WGSL Backend

Emitting WGSL text and the uniform-buffer layout. This backend consumes inlined, typed IR
and produces a [`WgslModule`](#pdum.dsl_reference.backends.wgsl.compile); the
[runtime](runtime.md) turns that into a real pipeline.

## Compile entry — `pdum.dsl_reference.backends.wgsl.compile`

::: pdum.dsl_reference.backends.wgsl.compile

## Uniform layout — `pdum.dsl_reference.backends.wgsl.layout`

::: pdum.dsl_reference.backends.wgsl.layout

## Emission — `pdum.dsl_reference.backends.wgsl.emit`

::: pdum.dsl_reference.backends.wgsl.emit

## Intrinsics & dialect tables — `pdum.dsl_reference.backends.wgsl.intrinsics`

::: pdum.dsl_reference.backends.wgsl.intrinsics
