"""pdum.dsl — the compiler-infrastructure core (design 200 §1.1).

Skeleton (migration P0): the kernel engine — capture, type dispatch, the
two-tier cache, the Node/Region IR, marshaling, the registry, the events
seam, the value language, the render walker, and the reference evaluator —
arrives at P1.

This file is the lockstep version anchor (scripts/_versioning.py).
"""

__version__ = "0.0.0+dev"
