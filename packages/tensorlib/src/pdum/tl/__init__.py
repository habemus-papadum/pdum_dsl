"""pdum.tl — the assemblage language and representation (design 200 §1.1).

Skeleton (migration P0): tensorlib — the layout algebra, carriers, the
compute primitives, the Program/Instr IR, reverse-mode AD, the transforms,
the cost semantics, placement, signatures, and the zoo — is promoted here
at P2 and converted onto pdum.dsl's caching, naming, and capture at P3.

Version of record: this distribution is version-locked with
``habemus-papadum-dsl`` (scripts/_versioning.py); the anchor lives in
``pdum.dsl.__version__``.
"""
